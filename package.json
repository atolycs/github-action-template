{
  "name": "github-action-template",
  "version": "1.0.0",
  "description": "Github Action Template for Node.js",
  "main": "index.js",
  "scripts": {
    "build:main": "esbuild --bundle --outdir=dist ./src/main.js",
    "build:post": "esbuild --bundle --outdir=dist ./src/post.js",
    "lint": "eslint .",
    "fix": "eslint . --fix"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.20.0",
    "eslint": "^8.56.0"
  },
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0"
  }
}
